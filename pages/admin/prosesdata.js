import { Typography } from '@mui/material';
import LayoutAdmin from '/components/Layout/Admin';

const ProsesData = () => {
  return (
    <>
      <LayoutAdmin pageTitle="Proses Data">
        <Typography>Proses Data</Typography>
      </LayoutAdmin>
    </>
  );
};

export default ProsesData;
